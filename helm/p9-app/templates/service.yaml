apiVersion: v1
kind: Service
metadata:
  name: {{ include "p9-app.fullname" . }}
  labels:
    {{- include "p9-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
  selector:
    {{- include "p9-app.selectorLabels" . | nindent 4 }}
